import React from 'react';
import MSTag from "../MSTag";

interface InfoProps {
  data: any;
  metadata: any;
  children?: React.ReactNode | string;
}

const Info: React.FC<InfoProps> = ({ data, metadata, children }) => {
  return (
    <></>
  );
};

export default Info;