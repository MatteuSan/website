import { works } from "../../constants/works";
import MarkdownLayout from "../../layouts/MarkdownLayout";

export const metadata = {
  title: 'BlockCorp',
  description: 'The platform to order Growtopia-related services.',
}

export const WorkInfo = works.filter(item => item.name === metadata.title)[0];

The platform to order Growtopia-related services.

export default function WorkPage({ children }) {
  return (
    <MarkdownLayout
      data={ WorkInfo }
      metadata={ metadata }
      previewImage="blockcorp.webp"
      previewImageAlt="Blockcorp's landing page."
      links={(
        <li><p className="body">There are no links for this project.</p></li>
      )}
    >
      { children }
    </MarkdownLayout>
  );
}