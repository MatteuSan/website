import DefaultLayout from "../../layouts/DefaultLayout";

import { MSAnimatedSection, MSButton, MSTag, MSTitleBar } from "../../components";
import {
  ArrowLeftIcon,
  PhotoIcon,
  QuestionMarkCircleIcon
} from "@heroicons/react/24/outline";
import { SiGithub } from "@icons-pack/react-simple-icons";
import PreviewImage from "../../components/markdown/PreviewImage";
import { works } from "../../constants/works";

export const metadata = {
  title: 'Dev Protocol',
  description: 'Decentralized funding for sustainable creator economy.',
}

export const WorkInfo = works.filter(item => item.name === metadata.title)[0];

<MSAnimatedSection>
  # { metadata.title }
</MSAnimatedSection>
<MSAnimatedSection delay={0.3}>
  ### { metadata.description }
</MSAnimatedSection>

<MSAnimatedSection delay={0.4}>
  <div className="w-full h-full max-h-md r-lg my-md" style={{ overflow: 'hidden'}}>
    <PreviewImage src="/img/devprotocol.webp" alt="Dev Protocol's landing page." />
  </div>

  Dev Protocol is a solution for sustainability issues of creators activities, it is designed for the blockchain solution and enhance creators to grow their economy. During my time with the company we've made a lot of significant improvements with how they handle user interfaces. It gave birth to their design system, [Hashi](/tools/hashi). With its implementation, the new unified design language now spans across multiple products in the ecosystem. It also laid the foundation for their numerous products including [Niwa](https://niwa.xyz), [Clubs](https://clubs.place), and their main website.
</MSAnimatedSection>

<MSAnimatedSection className="grid cols-1 @large:cols-3 gap-md mt-4xl">
  <MSAnimatedSection>
    <MSTitleBar clsssName="jc-center" icon={<QuestionMarkCircleIcon />}>Info</MSTitleBar>
    <div className="w-full start-1 end-1 fill-surface-400 ink-surface-ink p-lg r-md raise-low mt-md" style={{ height: 'max-content' }}>
      <ul className="ms-list mb-md">
        <li className="ms-list__item">Client: **FRAME00**</li>
        <li className="ms-list__item">Status: **{ WorkInfo.status }**</li>
        <li className="ms-list__item mt-sm">{ WorkInfo.tags.map((item, i) => <MSTag key={ i } label={ item }/>) }</li>
      </ul>
      #### Links
      <div className="flex flow-row flow-nowrap jc-start gap-sm mt-sm">
        <MSButton link="https://devprotocol.xyz" type="small filled">Website</MSButton>
        <MSButton link="https://github.com/dev-protocol" type="small filled" icon={['left', <SiGithub />]}>GitHub</MSButton>
      </div>
    </div>
  </MSAnimatedSection>
  <MSAnimatedSection delay={0.3} className="start-1 end-1 @large:start-2 @large:end-4">
    <MSTitleBar clsssName="jc-center" icon={<PhotoIcon />}>Media</MSTitleBar>
    <div className="w-full grid cols-1 gap-md mt-md">
      <PreviewImage src="/img/devprotocol/devprotocol-clubs.webp" alt="Dev Protocol's Clubs." />
      <PreviewImage src="/img/devprotocol/devprotocol-clubs-dashboard.webp" alt="Dev Protocol's Clubs dashboard." />
      <PreviewImage src="/img/devprotocol/devprotocol-niwa.webp" alt="Dev Protocol's Niwa." />
      <PreviewImage src="/img/devprotocol/devprotocol-stakes-social.webp" alt="Dev Protocol's stakes.social." />
    </div>
  </MSAnimatedSection>
</MSAnimatedSection>

export default function WorkPage({ children }) {
  return (
    <DefaultLayout title={metadata.title} description={metadata.description} previewImage="/img/devprotocol.webp">
      <section className="py-4xl">
        <MSButton style={{ position: 'relative', left: '-0.9rem', marginBottom: '0.7rem' }} link="/work" type="small" icon={['left', <ArrowLeftIcon />]}>Back to all work</MSButton>
        {children}
      </section>
    </DefaultLayout>
  );
}