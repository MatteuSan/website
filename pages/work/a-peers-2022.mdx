import { MSButton } from "../../components";
import PreviewImage from '../../components/markdown/PreviewImage';
import { works } from "../../constants/works";
import MarkdownLayout from "../../layouts/MarkdownLayout";

export const metadata = {
  title: 'A-PEERS 2022',
  description: 'Website in celebration of the National Arts Month 2022 in RLSAA.',
}

export const WorkInfo = works.filter(item => item.name === metadata.title)[0];

A website for the A-PEER! 2022 program in the Regional Lead School for the Arts in Angono. This project was made in collaboration with Luwal Sining-Pagganap, Mediarama Productions, and the RLSAA Applied Arts Department. It showcases the mid-year works of each senior high school department has created.

export default function WorkPage({ children }) {
  return (
    <MarkdownLayout
      data={ WorkInfo }
      metadata={ metadata }
      previewImage="apeers.webp"
      previewImageAlt="The A-PEER 2022 landing page."
      links={(
        <>
          <li><MSButton link={ WorkInfo['link'] } type="small outlined">Website</MSButton></li>
        </>
      )}
      media={(
        <>
          <PreviewImage src="/img/apeers/apeers-gallery.png" alt="The art gallery page." />
          <PreviewImage src="/img/apeers/apeers-preview.png" alt="The individual showcase page." />
        </>
      )}
    >
      { children }
    </MarkdownLayout>
  );
}