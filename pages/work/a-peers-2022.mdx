import { MSButton } from "../../components";
import { works } from "../../constants/works";
import MarkdownLayout from "../../layouts/MarkdownLayout";

export const metadata = {
  title: 'A-PEERS 2022',
  description: 'Website in celebration of the National Arts Month 2022 in RLSAA.',
}

export const WorkInfo = works.filter(item => item.name === metadata.title)[0];

A website for the A-PEER! 2022 program in the Regional Lead School for the Arts in Angono. It has a gallery of the participating students' works.

export default function WorkPage({ children }) {
  return (
    <MarkdownLayout
      data={ WorkInfo }
      metadata={ metadata }
      previewImage="apeers.webp"
      previewImageAlt="The A-PEER 2022 landing page."
      links={<MSButton link={ WorkInfo['link'] } type="small outlined">Website</MSButton>}
    >
      { children }
    </MarkdownLayout>
  );
}