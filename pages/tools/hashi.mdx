import DefaultLayout from "../../layouts/DefaultLayout";
import Image from "next/image";

import { tools } from '../../constants/tools';
import { MSAnimatedSection, MSButton, MSTag, MSTitleBar } from "../../components";
import {
  ArrowLeftIcon,
  BookOpenIcon,
  PaintBrushIcon,
  PhotoIcon,
  QuestionMarkCircleIcon
} from "@heroicons/react/24/outline";
import { SiGithub } from "@icons-pack/react-simple-icons";
import PreviewImage from "../../components/markdown/PreviewImage";

export const metadata = {
  title: 'Hashi',
  description: 'Dev Protocol\'s design system for building bridges from software to user.',
}

export const HashiInfo = tools.filter(tool => tool.name === 'Hashi')[0];

<MSAnimatedSection>
  # Hashi
</MSAnimatedSection>
<MSAnimatedSection delay={0.3}>
  ### Dev Protocol's design system for building bridges from software to user.
</MSAnimatedSection>

<MSAnimatedSection delay={0.4}>
  <div className="w-full h-full max-h-md r-lg my-md" style={{ overflow: 'hidden'}}>
    <PreviewImage src="/img/hashi/hashi-cover.webp" alt="Hashi's cover image." />
  </div>

  Hashi is Dev Protocol's design system. Aiming to bring a unified user experience when using Dev Protocol products, to present an elegant user interface for consumers to interact with, and to be able to create the same user interfaces regardless of any frontend framework.
</MSAnimatedSection>

<MSAnimatedSection className="grid cols-1 @large:cols-3 gap-md mt-4xl">
  <MSAnimatedSection>
    <MSTitleBar style={{ justifyContent: 'center' }} icon={<QuestionMarkCircleIcon />}>Info</MSTitleBar>
    <div className="w-full start-1 end-1 fill-surface-400 ink-surface-ink p-lg r-md raise-low mt-md" style={{ height: 'max-content' }}>
      <ul className="ms-list mb-md">
        <li className="ms-list__item">Client: [Dev Protocol](https://devprotocol.xyz)</li>
        <li className="ms-list__item">Built with { HashiInfo.tags.map((item, i) => <MSTag key={ i } label={ item }/>) }</li>
      </ul>
      #### Links
      <div className="flex flow-row flow-nowrap jc-start gap-sm mt-sm">
        <MSButton link={HashiInfo.links['docs']} icon={['left', <BookOpenIcon />]} type="small filled">Docs</MSButton>
        <MSButton link={HashiInfo.links['figma']} icon={['left', <PaintBrushIcon />]} type="small filled">Figma</MSButton>
        <MSButton link={HashiInfo.links['src']} icon={['left', <SiGithub />]} type="small filled">Source</MSButton>
      </div>
    </div>
  </MSAnimatedSection>
  <MSAnimatedSection delay={0.3} className="start-1 end-1 @large:start-2 @large:end-4">
    <MSTitleBar style={{ justifyContent: 'center' }} icon={<PhotoIcon />}>Media</MSTitleBar>
    <div className="w-full grid cols-1 gap-md mt-md">
      <PreviewImage src="/img/hashi.webp" alt="A screen capture of Hashi's website" />
      <PreviewImage src="/img/hashi/hashi-cover--dark.webp" alt="Hashi's cover image but in dark mode." />
    </div>
  </MSAnimatedSection>
</MSAnimatedSection>

export default function HashiPage({ children }) {
  return (
    <DefaultLayout title={metadata.title} description={metadata.description} previewImage="/img/hashi/hashi-cover.webp">
      <section className="py-4xl">
        <MSButton style={{ position: 'relative', left: '-0.9rem', marginBottom: '0.7rem' }} link="/tools" type="small" icon={['left', <ArrowLeftIcon />]}>Back to all tools</MSButton>
        {children}
      </section>
    </DefaultLayout>
  );
}