import { tools } from '../../constants/tools';
import { MSButton } from "../../components";
import PreviewImage from "../../components/markdown/PreviewImage";
import MarkdownLayout from "../../layouts/MarkdownLayout";

export const metadata = {
  title: 'Sentro',
  description: 'A low-level SCSS library for building and managing token-driven design systems.',
}

export const ToolInfo = tools.filter(tool => tool.name === metadata.title)[0];

Sentro is a low-level library for building token-driven design systems. It features token and key management, a way to create themeable and scalable components, helpful tools to improve the developer experiece, a way to manage themes, and create consistent and maintainable design systems.

export default function ToolPage({ children }) {
  return (
    <MarkdownLayout
      data={ ToolInfo }
      metadata={ metadata }
      previewImage="sentro/sentro.webp"
      previewImageAlt="Sentro's cover image."
      media={(
        <>
          <PreviewImage src="/img/sentro/sentro-sample-code.webp" alt="A design system using Sentro as its core." />
        </>
      )}
      links={(
        <>
          <MSButton link={ToolInfo.links['docs']} type="small filled">Docs</MSButton>
          <MSButton link={ToolInfo.links['src']} type="small filled">Source</MSButton>
        </>
      )}
    >
      { children }
    </MarkdownLayout>
  );
}